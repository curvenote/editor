import { SuggestionState, SuggestionActionTypes } from './types';
declare const suggestionReducer: (state: SuggestionState | undefined, action: SuggestionActionTypes) => SuggestionState;
export default suggestionReducer;
